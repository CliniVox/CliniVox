<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/scrollViewSettings"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient"
    android:fillViewport="true">

    <LinearLayout
        android:id="@+id/layout_settings"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="32dp"
        android:gravity="center_horizontal">

        <!-- Botão Voltar com ícone e sombra -->
        <LinearLayout
            android:id="@+id/btnVoltar"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            android:orientation="horizontal"
            android:background="@drawable/bg_rounded_ripple_blue"
            android:paddingStart="20dp"
            android:paddingEnd="20dp"
            android:gravity="center_vertical"
            android:clickable="true"
            android:focusable="true"
            android:elevation="4dp"
            android:layout_gravity="start"
            android:layout_marginTop="16dp">

            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:src="@android:drawable/ic_media_previous"
                android:tint="#42A5F5"
                android:contentDescription="Voltar" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="VOLTAR"
                android:textColor="#42A5F5"
                android:textSize="16sp"
                android:fontFamily="sans-serif-medium"
                android:layout_marginStart="8dp"
                android:letterSpacing="0.05" />
        </LinearLayout>

        <!-- Título Ajustes -->
        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Ajustes"
            android:textSize="38sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:fontFamily="sans-serif-medium"
            android:layout_marginTop="56dp" />

        <!-- Container para botões/link com sombra e cantos arredondados -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_marginTop="64dp"
            android:gravity="center_horizontal"
            android:paddingHorizontal="16dp">

            <!-- Botão Sobre Nós -->
            <TextView
                android:id="@+id/btnSobreNos"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:background="@drawable/bg_card_light_ripple"
                android:gravity="center"
                android:text="Sobre nós"
                android:textColor="#42A5F5"
                android:textSize="18sp"
                android:fontFamily="sans-serif-medium"
                android:clickable="true"
                android:focusable="true"
                android:letterSpacing="0.02"
                android:textAllCaps="false"
                android:elevation="3dp"
                android:layout_marginBottom="24dp" />

            <!-- Botão Sair -->
            <TextView
                android:id="@+id/btnSair"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:background="@drawable/bg_card_light_ripple"
                android:gravity="center"
                android:text="Sair"
                android:textColor="#42A5F5"
                android:textSize="18sp"
                android:fontFamily="sans-serif-medium"
                android:clickable="true"
                android:focusable="true"
                android:letterSpacing="0.02"
                android:textAllCaps="false"
                android:elevation="3dp" />
        </LinearLayout>

        <!-- Texto inferior CliniVox -->
        <TextView
            android:id="@+id/tvLogo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="CliniVox"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#BBDEFB"
            android:fontFamily="sans-serif-medium"
            android:layout_marginTop="96dp" />
    </LinearLayout>
</ScrollView>
